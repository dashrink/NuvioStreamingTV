fn main() {
    // UniFFI scaffolding generated automatically via #[uniffi::export] macros
    // and uniffi::setup_scaffolding!() in lib.rs
    // This build script ensures proper rebuild triggers
    println!("cargo:rerun-if-changed=src/");
}
